[package]
name = "navara_math"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_math = "0.14.1"
bevy_transform = "0.14.1"
cfg-if = "1.0.0"

[features]
default = ["use_f32"]
use_f32 = []
use_f64 = []
