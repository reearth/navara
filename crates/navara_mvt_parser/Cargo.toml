[package]
name = "navara_mvt_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
mvt-reader = { git = "https://github.com/reearth/mvt-reader.git" }
geo-types = "0.7"

[dev-dependencies]
prost = "0.11"