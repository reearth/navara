[package]
name = "navara_pmtiles"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
# TEMPORARY (step 1 of the PMTiles refactor): this crate is being repurposed
# from "pure parser" into the Bevy-integration crate. For now it re-exports the
# extracted `pmtiles` crate so dependents keep building; step 3 replaces this.
pmtiles.workspace = true
