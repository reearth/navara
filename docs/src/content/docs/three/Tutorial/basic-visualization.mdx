---
title: Basic Visualization
description: Basic implementation of map display, terrain display, and GIS data display
sidebar:
  order: 200
---

import { Tabs, TabItem } from "@astrojs/starlight/components";

## Setup

This tutorial assumes a TypeScript project with a bundler such as [Vite](https://vite.dev/). Install the Navara packages and their peer dependencies:

<Tabs syncKey="pkg">

<TabItem label="npm">

```bash
npm install @navaramap/three @navaramap/three-default-plugin @navaramap/three-plugins three postprocessing
```

</TabItem>

<TabItem label="yarn">

```bash
yarn add @navaramap/three @navaramap/three-default-plugin @navaramap/three-plugins three postprocessing
```

</TabItem>

<TabItem label="pnpm">

```bash
pnpm add @navaramap/three @navaramap/three-default-plugin @navaramap/three-plugins three postprocessing
```

</TabItem>

</Tabs>

## Displaying a Map

### Adding a Raster Layer

Create `index.html` at the project root with the following content:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Navara Three</title>
  </head>
  <body style="margin: 0; overflow: hidden">
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

Next, create `src/main.ts` with the following content:

```typescript
import ThreeView from "@navaramap/three";
import { DefaultPlugin, type DefaultDescriptions } from "@navaramap/three-default-plugin";
import { TileJsonPlugin } from "@navaramap/three-plugins";

const plugin = new DefaultPlugin();
const view = new ThreeView<DefaultDescriptions>({});
view.addPlugin(plugin);

const tilejson = new TileJsonPlugin();
view.addPlugin(tilejson);

await view.init();
```

`view.init()` builds the 3D scene and camera, and plugins can only be registered **before** it, and `addPlugin()` throws afterwards. Registering `DefaultPlugin` is what makes built-in Descriptors such as `ambient` valid in the next step.

Add the following code to `main.ts`:

```typescript
view.addLight({
  ambient: {},
});

const basemapSource = await tilejson.addSource({
  type: "raster-tile",
  url: "https://papers.reearth.land/styles/protomaps-light/tilejson.json",
});
view.addLayer({
  type: "raster",
  source: basemapSource,
});
```

Ambient light alone is all a raster basemap needs. The basemap comes from a [Re:Earth Papers](https://papers.reearth.land) TileJSON document. `TileJsonPlugin.addSource()` fetches it and derives the tile URL template, zoom range, and attribution. See [TileJsonPlugin](../../../three_plugins/tilejsonplugin/) for the full mapping.

### Result

A base map will be displayed on the globe in the scene.

![Basic Map](@assets/tutorial/basemap.png)

### Complete Code

```typescript
import ThreeView from "@navaramap/three";
import { DefaultPlugin, type DefaultDescriptions } from "@navaramap/three-default-plugin";
import { TileJsonPlugin } from "@navaramap/three-plugins";

const plugin = new DefaultPlugin();
const view = new ThreeView<DefaultDescriptions>({});
view.addPlugin(plugin);

const tilejson = new TileJsonPlugin();
view.addPlugin(tilejson);

await view.init();

view.addLight({
  ambient: {},
});

const basemapSource = await tilejson.addSource({
  type: "raster-tile",
  url: "https://papers.reearth.land/styles/protomaps-light/tilejson.json",
});
view.addLayer({
  type: "raster",
  source: basemapSource,
});
```

## Setting the Camera Position

To display the map at a specific location, set the camera position. Add the following to `main.ts`:

```typescript
view.setCamera({
  lng: 139.7671,
  lat: 35.6812,
  height: 1000,
  heading: 0, // -180 to 180
  pitch: -30, // -180 to 0
  roll: 0, // -180 to 180
});
```

For the full parameter list, see [ThreeView Functions](../../../three/api/threeview-functions/).

### Result

The camera position is set to the area around Tokyo.

![Camera Map](@assets/tutorial/camera.png)

### Complete Code

```typescript
import ThreeView from "@navaramap/three";
import { DefaultPlugin, type DefaultDescriptions } from "@navaramap/three-default-plugin";
import { TileJsonPlugin } from "@navaramap/three-plugins";

const plugin = new DefaultPlugin();
const view = new ThreeView<DefaultDescriptions>({});
view.addPlugin(plugin);

const tilejson = new TileJsonPlugin();
view.addPlugin(tilejson);

await view.init();

view.addLight({
  ambient: {},
});

const basemapSource = await tilejson.addSource({
  type: "raster-tile",
  url: "https://papers.reearth.land/styles/protomaps-light/tilejson.json",
});
view.addLayer({
  type: "raster",
  source: basemapSource,
});

view.setCamera({
  lng: 139.7671,
  lat: 35.6812,
  height: 1000,
  heading: 0, // -180 to 180
  pitch: -30, // -180 to 0
  roll: 0, // -180 to 180
});
```

## Displaying Terrain

### Adding a Terrain Layer

Terrain comes from a `quantized-mesh` source: pre-meshed tiles that carry their own geometry, so there is no elevation encoding to decode. Requesting per-vertex normals lets the mesh be lit directly, which removes the need for a separate hillshade layer.

Add the terrain layer **before** the raster tile layer (layers are rendered in the order they are added):

```typescript
const terrainSource = view.addSource({
  type: "quantized-mesh",
  url: "https://terrain.reearth.land/cesium-mesh/ellipsoid/{z}/{x}/{y}.terrain",
  requestVertexNormals: true,
  maxZoom: 18,
});
view.addLayer({
  type: "terrain",
  source: terrainSource,
  terrain: {
    castShadow: true,
    receiveShadow: true,
  },
});
```

The data is the global quantized-mesh dataset published by [Re:Earth Terrain](https://terrain.reearth.land/).

- `requestVertexNormals` asks the server for vertex normals, which let the scene lighting shade the terrain. Add a `sun` light when you request them.
- Fetch settings such as `maxZoom` live on the source. `terrain` holds only how the mesh is rendered (shadows, skirts).

For details, see [Terrain Layer](../../../three/layer/terrain-layer/).

### Result

When you tilt the map, you can see the terrain relief.

![Terrain Map](@assets/tutorial/terrain.png)

### Complete Code

```typescript
import ThreeView from "@navaramap/three";
import { DefaultPlugin, type DefaultDescriptions } from "@navaramap/three-default-plugin";
import { TileJsonPlugin } from "@navaramap/three-plugins";

const plugin = new DefaultPlugin();
const view = new ThreeView<DefaultDescriptions>({});
view.addPlugin(plugin);

const tilejson = new TileJsonPlugin();
view.addPlugin(tilejson);

await view.init();

view.addLight({
  ambient: {},
});

const terrainSource = view.addSource({
  type: "quantized-mesh",
  url: "https://terrain.reearth.land/cesium-mesh/ellipsoid/{z}/{x}/{y}.terrain",
  requestVertexNormals: true,
  maxZoom: 18,
});
view.addLayer({
  type: "terrain",
  source: terrainSource,
  terrain: {
    castShadow: true,
    receiveShadow: true,
  },
});

const basemapSource = await tilejson.addSource({
  type: "raster-tile",
  url: "https://papers.reearth.land/styles/protomaps-light/tilejson.json",
});
view.addLayer({
  type: "raster",
  source: basemapSource,
});

view.setCamera({
  lng: 139.7671,
  lat: 35.6812,
  height: 1000,
  heading: 0, // -180 to 180
  pitch: -30, // -180 to 0
  roll: 0, // -180 to 180
});
```

## Displaying GeoJSON Data

### Adding a GeoJSON Layer

```typescript
const geojsonSource = view.addSource({
  type: "geojson",
  data: {
    type: "Feature",
    properties: { name: "Area" },
    geometry: {
      type: "Polygon",
      coordinates: [
        [
          [139.75843063805576, 35.70688252862743],
          [139.75843063805576, 35.700933240062355],
          [139.77157543771887, 35.700933240062355],
          [139.77157543771887, 35.70688252862743],
          [139.75843063805576, 35.70688252862743],
        ],
      ],
    },
  },
});
view.addLayer({
  type: "vector",
  source: geojsonSource,
  polygon: {
    color: new Color().setHex(0xff3b30),
    height: 0,
    opacity: 0.75,
    transparent: true,
  },
});
```

Register the GeoJSON as a source with `view.addSource({ type: "geojson", ... })`, then draw it with `view.addLayer({ type: "vector", source, ... })`. The `polygon` material carries the styling such as colour, height, and transparency. For details, see [Vector Layer](../../../three/layer/vector-layer/).

### Result

A polygon is displayed on the map.

![GeoJSON Map](@assets/tutorial/geojson.png)

### Complete Code

```typescript
import ThreeView, { Color } from "@navaramap/three";
import { DefaultPlugin, type DefaultDescriptions } from "@navaramap/three-default-plugin";
import { TileJsonPlugin } from "@navaramap/three-plugins";

const plugin = new DefaultPlugin();
const view = new ThreeView<DefaultDescriptions>({});
view.addPlugin(plugin);

const tilejson = new TileJsonPlugin();
view.addPlugin(tilejson);

await view.init();

view.addLight({
  ambient: {},
});

const terrainSource = view.addSource({
  type: "quantized-mesh",
  url: "https://terrain.reearth.land/cesium-mesh/ellipsoid/{z}/{x}/{y}.terrain",
  requestVertexNormals: true,
  maxZoom: 18,
});
view.addLayer({
  type: "terrain",
  source: terrainSource,
  terrain: {
    castShadow: true,
    receiveShadow: true,
  },
});

const basemapSource = await tilejson.addSource({
  type: "raster-tile",
  url: "https://papers.reearth.land/styles/protomaps-light/tilejson.json",
});
view.addLayer({
  type: "raster",
  source: basemapSource,
});

const geojsonSource = view.addSource({
  type: "geojson",
  data: {
    type: "Feature",
    properties: {},
    geometry: {
      type: "Polygon",
      coordinates: [
        [
          [139.75843063805576, 35.70688252862743],
          [139.75843063805576, 35.700933240062355],
          [139.77157543771887, 35.700933240062355],
          [139.77157543771887, 35.70688252862743],
          [139.75843063805576, 35.70688252862743],
        ],
      ],
    },
  },
});
view.addLayer({
  type: "vector",
  source: geojsonSource,
  polygon: {
    color: new Color().setHex(0xff3b30),
    height: 0,
    opacity: 0.75,
    transparent: true,
  },
});

view.setCamera({
  lng: 139.7671,
  lat: 35.6812,
  height: 1000,
  heading: 0, // -180 to 180
  pitch: -30, // -180 to 0
  roll: 0, // -180 to 180
});
```
