---
// Starlight ThemeSelect override: renders nothing — light mode is disabled
// for now, so there is no theme to select (see ThemeProvider.astro). This
// also removes the picker from the mobile menu footer.
---
