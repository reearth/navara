# @navaramap/worker

This is a worker module for Navara for Web platform. It provides a Web Worker pool and task manager that run heavy processing (such as vector tile parsing) off the main thread using Navara's WASM worker module.
